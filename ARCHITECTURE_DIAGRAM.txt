Multilingual Dynamic STT System - Architecture Diagram
=======================================================

┌───────────────────────────────────────────────────────────────────────┐
│                        AUDIO INPUT (Microphone)                        │
└────────────────────────────────┬──────────────────────────────────────┘
                                 │
                                 ▼
┌───────────────────────────────────────────────────────────────────────┐
│                      STT SERVICE ORCHESTRATOR                          │
│  (stt_service.py)                                                      │
│                                                                        │
│  State:                                                                │
│  • current_stt_service = "vosk" or "whisper"                          │
│  • current_language = "en", "es", etc.                                │
│  • audio_buffer = [3 seconds of audio]                                │
│                                                                        │
│  Logic:                                                                │
│  1. Buffer incoming audio                                              │
│  2. Every 10s: Detect language via Whisper                            │
│  3. Route audio to appropriate service                                 │
│  4. Switch service/model based on detection                            │
└─────┬────────────────────────────┬────────────────────────────────────┘
      │                            │
      │ Audio Routing              │ Language Detection
      │ (based on state)           │ (background thread)
      │                            │
      ▼                            ▼
┌─────────────────────┐     ┌──────────────────────────────┐
│   VOSK SERVICE      │     │     WHISPER SERVICE          │
│  (vosk_service.py)  │     │   (whisper_service.py)       │
│                     │     │                              │
│  Models Dict:       │     │  Functions:                  │
│  {                  │     │  • detect_language()         │
│   "en": Model_EN,   │     │    → (lang_code, confidence) │
│   "es": Model_ES,   │     │  • transcribe()              │
│   "fr": Model_FR,   │     │    → transcription text      │
│   "de": Model_DE    │     │                              │
│  }                  │     │  Used when:                  │
│                     │     │  • Language detection needed │
│  Current: "en"      │     │  • Vosk unavailable for lang │
│  Switch: O(1)       │     │  • Transcription fallback    │
│  Thread-safe        │     │                              │
└──────┬──────────────┘     └───────────┬──────────────────┘
       │                                │
       │ UDP Transcription              │ UDP Transcription
       │                                │
       ▼                                ▼
┌────────────────────────────────────────────────────────────┐
│                    UDP HANDLER                              │
│                  (udp_handler.py)                           │
│                                                             │
│  Vosk Ports:         Whisper Ports:      YAMNet Port:      │
│  • 7201 (partial)    • 7211 (partial)    • 7204 (events)  │
│  • 7202 (final)      • 7212 (final)                       │
│  • 7203 (word conf)                                        │
└──────────────────────────────┬─────────────────────────────┘
                               │
                               ▼
                    ┌──────────────────┐
                    │  TOUCHDESIGNER   │
                    │  or UDP Client   │
                    └──────────────────┘


LANGUAGE DETECTION FLOW
=======================

┌──────────────────────────────────────────────────────────────┐
│ Time: 0s                                                      │
│ Action: System starts, loads all Vosk models                 │
│ State: current_language = None, current_stt_service = "vosk"│
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Time: 0-3s                                                    │
│ Action: Buffer audio (3 seconds)                             │
│ State: audio_buffer fills up                                 │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Time: 3s                                                      │
│ Action: First language detection                             │
│ Whisper: detect_language(buffer) → ("en", 0.92)             │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Decision: Language detected with high confidence             │
│ Check: Is "en" in vosk_models? → YES                        │
│ Action: vosk_service.switch_language("en")                   │
│ State: current_language = "en", current_stt_service = "vosk"│
│ Log: "🔄 Switched language: None → en"                      │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Time: 3-13s                                                   │
│ Action: Transcribe via Vosk English model                    │
│ Output: Real-time transcription to UDP 7201, 7202, 7203     │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Time: 13s                                                     │
│ Action: Second language detection                            │
│ Whisper: detect_language(buffer) → ("es", 0.88)             │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Decision: Language changed!                                   │
│ Check: Is "es" in vosk_models? → YES                        │
│ Action: vosk_service.switch_language("es")                   │
│ State: current_language = "es", current_stt_service = "vosk"│
│ Log: "🔄 Switched language: en → es"                        │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Time: 13-23s                                                  │
│ Action: Transcribe via Vosk Spanish model                    │
│ Output: Real-time transcription to UDP 7201, 7202, 7203     │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Time: 23s                                                     │
│ Action: Third language detection                             │
│ Whisper: detect_language(buffer) → ("ja", 0.85)             │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Decision: Language changed to unsupported language           │
│ Check: Is "ja" in vosk_models? → NO                         │
│ Action: Switch to Whisper service                            │
│ State: current_language = "ja", current_stt_service="whisper"│
│ Log: "✅ Switched to Whisper (ja)"                          │
└──────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌──────────────────────────────────────────────────────────────┐
│ Time: 23s+                                                    │
│ Action: Transcribe via Whisper (all languages supported)     │
│ Output: Transcription to UDP 7211, 7212                      │
└──────────────────────────────────────────────────────────────┘


THREADING MODEL
===============

┌─────────────────────────────────────────────────────────────┐
│                        MAIN THREAD                           │
│  • Starts orchestrator                                       │
│  • Initializes all services                                  │
│  • Sets up audio stream                                      │
│  • Audio callback: _audio_callback()                         │
│    - Buffers audio                                           │
│    - Routes to active STT service                            │
│    - Triggers detection when needed                          │
└─────────────────────────────────────────────────────────────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
                ▼               ▼               ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │ DETECTION    │  │ VOSK SERVICE │  │ WHISPER SVC  │
    │ THREAD       │  │ THREAD       │  │ THREAD       │
    │ (background) │  │ (daemon)     │  │ (daemon)     │
    │              │  │              │  │              │
    │ • Language   │  │ • Audio queue│  │ • Audio queue│
    │   detection  │  │ • Recognize  │  │ • Transcribe │
    │ • Non-block  │  │ • UDP send   │  │ • UDP send   │
    └──────────────┘  └──────────────┘  └──────────────┘


CONFIGURATION FILES
===================

vosk_models.csv:
┌──────────────────────────────────────┐
│ Key │ Value                          │
│─────┼────────────────────────────────│
│ en  │ models/vosk-model-en-us-0.15   │
│ es  │ models/vosk-model-es-0.42      │
│ fr  │ models/vosk-model-fr-0.22      │
│ de  │ models/vosk-model-de-0.15      │
│ pt  │ models/vosk-model-pt-0.3       │
└──────────────────────────────────────┘
        ↓
    Loaded at startup
        ↓
    Stored in memory
        ↓
    Switched dynamically

orchestrator_config.csv:
┌────────────────────────────────────────┐
│ DYNAMIC_SWITCHING_ENABLED = true       │
│ LANGUAGE_DETECT_BUFFER_DURATION = 3.0  │
│ LANGUAGE_CONFIDENCE_THRESHOLD = 0.5    │
│ LANGUAGE_DETECT_INTERVAL = 10.0        │
└────────────────────────────────────────┘
        ↓
    Controls behavior
        ↓
    Tunable parameters
        ↓
    No code changes needed


KEY FEATURES SUMMARY
====================

✅ Multiple preloaded Vosk models
✅ O(1) language switching
✅ Thread-safe operations
✅ Non-blocking detection
✅ Intelligent fallback
✅ Configurable thresholds
✅ Comprehensive logging
✅ UDP output unchanged
✅ TouchDesigner compatible
✅ Production ready
